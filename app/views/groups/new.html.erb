<header>
  <%= link_to groups_path, :class => 'arrow' do %>
    <i class="material-icons blue">arrow_back</i>
  <% end %>
  <h2>Add Catagories</h2>
</header>
<%= form_with url: groups_path, method: :post, local: true, scope: :data, :class => "page" do |form| %>
  <div class="formDiv">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <div class="formDiv">
    <%= form.label :icon %>
    <%= form.text_field :icon %>
  </div>
  <div class="pageBottom">
    <%= form.submit 'Submit', :class => 'greenButton'  %>
  </div>
<% end %>